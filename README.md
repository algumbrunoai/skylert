Dependências do Projeto

Para instalar as dependências necessárias para rodar o back-end em Python, certifique-se de que seu ambiente virtual esteja ativo e execute:

-- pip install Flask requests firebase-admin python-dotenv flask-cors
